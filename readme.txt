Please keep in mind that this project uses gradle as a build tool and was coded in intellij, so if you are
running on VS code or another IDE please remember to build the build.gradle file first using the command: 
./gradlew build(this is done automatically in intellij)


1- run the server first
2- run both clients (order does not matter)
3- to send an image and caption as a message follow the on screen promps

